{
  "name": "n3xus-archive",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"npm --prefix client start\" \"npm --prefix server start\"",
    "client": "npm --prefix client install && npm --prefix client start",
    "server": "npm --prefix server install && npm --prefix server start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.5.2"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
